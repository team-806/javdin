// Arithmetic
var result := (10 + 5) * 3 - 2 / 1

// Comparison operators
var tests := [
    5 < 10,
    10 > 5,
    10 >= 10,
    5 <= 5,
    10 = 10,
    10 /= 5,
    10 != 5
]

// Logical operators  
var logic := true and false or not true xor false
